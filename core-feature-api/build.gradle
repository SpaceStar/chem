apply {
    from "$rootDir/gradle/library.gradle"
}

android {
    namespace 'ru.spacestar.core_feature_api'

    defaultConfig {
        consumerProguardFiles "consumer-rules.pro"
    }
}

dependencies {
    compileOnly "androidx.navigation:navigation-compose:$nav_version"
}